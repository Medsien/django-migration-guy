on:
  workflow_dispatch:
    inputs:
      apps_path:
        description: 'Apps path'
        required: true
jobs:
  self-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./
        with:
          apps_path: ${{ github.event.inputs.apps_path }}
